# CLAUDE.md

This repository follows the same agent contract defined in `AGENTS.md`. Read `README.md` and the relevant files under `docs/` before making changes. Use `make test`, `make build`, and `make verify`; report exact results and never claim completion without evidence. This file is a compatibility pointer, not a vendor-specific workflow.
